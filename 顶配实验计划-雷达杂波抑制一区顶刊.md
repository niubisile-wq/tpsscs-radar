# 顶配实验计划：面向中科院一区顶刊的雷达杂波抑制论文方案

生成日期：2026-07-13  
研究方向：雷达杂波抑制、弱小目标检测、复值自监督学习、低虚警检测  
目标：在没有自采能力、不能申请授权数据集的条件下，设计一套尽可能支撑中科院一区顶刊投稿的完整实验方案。

## 0. 总体判断

当前最优路线不是单独提出一个深度网络，也不是只在 IPIX 或某个老数据集上刷指标。高概率冲击一区顶刊的策略应当是：

**方法创新 + 公开基准 + 低虚警强验证 + 物理可解释约束 + 完整开源复现。**

推荐主线：

**Physics-Guided Self-Supervised Complex-Valued Learning for Target-Preserving Radar Clutter Suppression Under Low-False-Alarm Constraints**

中文题目：

**低虚警约束下目标保持的物理引导自监督复值雷达杂波抑制**

建议方法名：

**TP-SSCS: Target-Preserving Self-Supervised Clutter Suppression**

建议基准名：

**OpenClutter-Bench: A Reproducible Public Benchmark for Target-Preserving Radar Clutter Suppression**

## 1. 目标期刊与论文定位

### 1.1 主投期刊

主投：

**IEEE Transactions on Geoscience and Remote Sensing, TGRS**

理由：

- TGRS 接收遥感仪器、雷达信号处理、图像/信号增强、分类、检测和解释类工作。
- 对公开数据、可复现基准、复杂场景泛化验证的接受度高。
- 如果方案强调跨公开数据集的遥感雷达杂波抑制与低虚警检测，比单纯 STAP 工程论文更适配。

官方页面：

https://www.grss-ieee.org/publications/transactions-on-geoscience-remote-sensing/

备投：

**IEEE Transactions on Aerospace and Electronic Systems, TAES**

理由：

- 雷达、STAP、检测、跟踪和航空电子方向更贴合。
- 如果最终论文更偏空载雷达/STAP，而不是遥感通用方法，TAES 更合适。

官方页面：

https://ieee-aess.org/publications/taes

### 1.2 不推荐作为第一目标的期刊

**Information Fusion**

除非把论文重构为多源、多模态、多雷达融合问题，否则单一雷达杂波抑制方法不够贴合。

**Remote Sensing**

可作为保底，但不是当前目标中的“一区顶刊”最优解。

### 1.3 论文类型

定位为：

**Algorithmic research article + public benchmark paper**

不是纯方法论文，也不是纯数据集论文。顶刊审稿人的核心问题是：

- 你的方法是否解决了雷达杂波抑制中的真实难点？
- 你的数据是否公开、可复现、足以证明泛化？
- 你的低虚警结果是否强于传统 STAP、低秩稀疏和深度学习基线？
- 你的创新是否有物理约束或理论解释，而不是普通网络堆叠？

## 2. 一句话论证

在公开雷达数据和有限标签条件下，本文证明一种物理引导的复值自监督低秩-稀疏分解网络能够在强非平稳杂波中抑制 clutter，同时保持弱小目标能量，并通过 AISTAP-SIM、RASPNet 公开子集、IPIX 和 NetRAD 的统一公开基准，在低虚警率条件下验证其相对 STAP、RPCA、SVD 和深度网络基线的优势。

## 3. 核心贡献设计

### 3.1 贡献 1：OpenClutter-Bench 公开基准

建立一个公开可复现的雷达杂波抑制基准，统一处理：

- 空载 STAP / GMTI 数据
- 真实海杂波数据
- 合成弱目标注入协议
- 固定低虚警检测协议
- 统一 baseline 复现实验
- 统一 train / validation / test split
- 统一 metrics 和统计检验脚本

这个贡献非常关键。没有自采数据时，公开基准就是论文可信度的核心资产。

### 3.2 贡献 2：TP-SSCS 物理引导复值自监督模型

模型不是普通 U-Net 去噪，而是显式建模：

\[
X = C + T + N
\]

其中：

- \(X\)：观测雷达复值回波或 range-Doppler / space-Doppler 张量
- \(C\)：杂波分量，具有低秩、子空间、重尾、非平稳特性
- \(T\)：弱小目标分量，稀疏、局部、跨多普勒/距离单元连续
- \(N\)：热噪声、量化噪声和残差项

方法核心：

- 复值网络保留 I/Q、相位和相干信息
- 低秩-稀疏 unrolling 模块约束 clutter/target 分解
- 重尾杂波似然处理海杂波 spike 和非高斯尾部
- 目标保持损失避免误杀慢速弱目标
- CFAR calibration head 约束输出检测统计量的虚警一致性

### 3.3 贡献 3：低虚警目标保持实验协议

普通 AUC 不足以说服雷达审稿人。本文主指标必须是：

- \(P_d\) at \(P_{fa}=10^{-2}\)
- \(P_d\) at \(P_{fa}=10^{-3}\)
- \(P_d\) at \(P_{fa}=10^{-4}\)
- empirical \(P_{fa}\) vs nominal \(P_{fa}\)
- target amplitude loss
- output SINR / SCNR gain

目标保持比单纯 clutter suppression 更有论文价值。

### 3.4 贡献 4：跨公开数据集泛化验证

主张不是“某个数据集上效果好”，而是：

- AISTAP-SIM 训练或主验证
- RASPNet public CVNN/EXAMPLES 外部验证
- IPIX 真实海杂波弱目标验证
- NetRAD 真实 S-band 单/双基地海杂波统计验证和目标注入压力测试

### 3.5 贡献 5：开源复现实验包

必须公开：

- 数据下载脚本
- 数据预处理脚本
- split 文件
- target injection 配置
- baseline 代码
- TP-SSCS 模型代码
- 训练配置
- 评估脚本
- 图表复现脚本
- 环境配置

没有开源，公开数据路线的优势会显著下降。

## 4. 数据集方案

### 4.1 主数据集 1：AISTAP-SIM

用途：

- 主定量验证
- 训练集
- 有 target-only 标签，适合目标保持损失和检测验证
- 空载 GMTI / STAP 场景核心数据

数据特征：

- GitHub 公开 release
- MIT 许可
- 包含模拟 radar data
- 包括 ground clutter、windy clutter、noise-only 等设置
- 支持 target-only ground truth

链接：

https://github.com/mit-ll/AISTAP-SIM

实验角色：

- 主训练数据
- 主表格结果
- 模型消融
- 目标保持量化
- 低虚警检测性能

### 4.2 主数据集 2：RASPNet public CVNN / EXAMPLES

用途：

- 空载自适应雷达外部泛化验证
- 不能依赖完整 16 TB 场景，因为完整版本需要联系和硬盘交换
- 只使用页面标明可 immediate download 的公开子集

链接：

https://www.sdms.afrl.af.mil/index.php?collection=raspnet

实验角色：

- 外部测试集
- 跨雷达场景泛化验证
- 模型是否过拟合 AISTAP-SIM 的关键证据

风险：

- 不要把完整 RASPNet 作为主实验依赖
- 论文中必须写清楚使用的是 public immediate-download subset

### 4.3 主数据集 3：IPIX sea clutter

用途：

- 经典真实 X-band 海杂波弱目标验证
- 适合验证真实海杂波、spike、低 SCR 目标和 CFAR 检测

参考页面：

http://soma.ece.mcmaster.ca/ipix/dartmouth/datasets.html

相关公开文献页面：

https://www.mdpi.com/2072-4292/11/23/2721

实验角色：

- 真实目标检测结果
- 与传统海杂波方法对比
- ROC / \(P_d@P_{fa}\) 验证

风险：

- 原始站点可能不稳定
- 必须提前下载、记录文件名、hash、访问日期
- 如果无法稳定获取，用 NetRAD + AISTAP-SIM 做主线，IPIX 降为补充验证

### 4.4 主数据集 4：NetRAD Monostatic / Bistatic Sea Clutter

用途：

- 新近公开真实 S-band 单/双基地海杂波
- 适合做真实海杂波统计、跨双基地角泛化、clutter-only false alarm calibration
- 如果无真实目标标签，则用于 target injection 和 false alarm 统计，不作为真实目标 detection 主证据

链接：

https://rdr.ucl.ac.uk/articles/dataset/NetRAD_-_Monostatic_Bistatic_Sea_Clutter_Dataset/32676582

实验角色：

- 外部真实海杂波验证
- 海杂波尾部分布验证
- 目标注入压力测试
- bistatic angle leave-one-out 泛化

风险：

- 数据体量大，约百 GB 级别
- 需要提前规划存储和下载

### 4.5 不纳入主线的数据集

NEXRAD：

- 优点是开放、规模大
- 但偏天气雷达 ground clutter / biological echo
- 与空载 STAP 和海杂波弱目标检测的主线不完全一致
- 建议作为下一篇或 appendix，不作为主实验

RadarScenes / automotive radar：

- 与道路毫米波 clutter 相关
- 但任务定义与空载/海杂波差异过大
- 不建议纳入主线，否则论文会变成“场景很多但问题发散”

## 5. 数据预处理和统一表示

### 5.1 统一输入格式

将所有数据统一为复值张量：

\[
X \in \mathbb{C}^{C \times R \times D}
\]

或根据数据形态使用：

\[
X \in \mathbb{C}^{C \times P \times R \times D}
\]

其中：

- \(C\)：通道、极化、阵元或接收机维度
- \(P\)：脉冲或 CPI 维度
- \(R\)：range bin
- \(D\)：Doppler bin

原则：

- 不只使用幅度图
- 保留 real / imaginary
- 保留 phase 信息
- 所有 normalization 只用训练集统计量
- 测试集不得参与均值方差估计

### 5.2 统一处理步骤

步骤：

1. 原始 I/Q 读取
2. 异常值检查和 NaN 检查
3. 按 CPI 切片
4. slow-time windowing
5. range-Doppler FFT
6. complex normalization
7. clutter ridge 标记
8. target mask 或 synthetic target mask 生成
9. train / validation / test split 固定保存
10. 输出 `.npz` 或 `.h5` 中间格式

### 5.3 数据划分原则

必须避免泄漏：

- 同一原始 recording 不能同时出现在 train 和 test
- 同一目标轨迹不能同时出现在 train 和 test
- 同一海况/角度的相邻片段不能随机打散到 train/test
- target injection 的随机种子必须固定并公开

推荐划分：

- AISTAP-SIM：按 scenario split
- RASPNet：按公开 example 类型 split
- IPIX：按 recording/date split
- NetRAD：按 bistatic angle 和 recording split

## 6. 目标注入协议

### 6.1 为什么需要目标注入

真实公开雷达数据经常缺少完整目标标签。为了系统评估 \(P_d\)、target preservation 和低虚警表现，需要受控目标注入。

目标注入不能替代真实目标验证，但可以提供：

- 可控 SCR
- 可控 Doppler offset
- 可控 range extent
- 可控 clutter ridge overlap
- 可重复统计显著性

### 6.2 注入参数

SCR 设置：

- -25 dB
- -20 dB
- -15 dB
- -10 dB
- -5 dB
- 0 dB

Doppler 位置：

- 远离 clutter ridge
- 靠近 clutter ridge
- 与 clutter ridge 部分重叠
- 慢速目标近零 Doppler

目标类型：

- point target
- extended target
- Swerling-I fluctuation
- Swerling-II fluctuation
- range migration target
- weak maneuvering target

每个设置至少 1000 个 samples，否则低虚警估计不稳定。

### 6.3 注入公式

\[
X_{inj} = X_{clutter} + \alpha A(\tau, f_d, \theta) s
\]

其中：

- \(X_{clutter}\)：真实或模拟 clutter-only 数据
- \(\alpha\)：由目标 SCR 决定
- \(A(\tau, f_d, \theta)\)：range-Doppler-angle steering response
- \(s\)：目标复包络

### 6.4 质量控制

每次注入保存：

- 原始 clutter file id
- injection seed
- SCR
- Doppler bin
- range bin
- target type
- target mask
- true target complex amplitude

## 7. TP-SSCS 方法设计

### 7.1 总体结构

TP-SSCS 包含 5 个核心模块：

1. Complex-valued front-end
2. Physics-guided low-rank clutter module
3. Sparse target-preserving module
4. Heavy-tailed clutter likelihood module
5. CFAR-calibrated detection head

### 7.2 Complex-valued front-end

输入：

\[
X = X_R + jX_I
\]

使用复值卷积或等价 real-imag coupling：

\[
Y_R = W_R * X_R - W_I * X_I
\]

\[
Y_I = W_R * X_I + W_I * X_R
\]

目的：

- 保留相干相位信息
- 避免幅度图去噪损坏弱目标相位结构
- 支持 STAP / RD 场景中的复数统计

### 7.3 Low-rank clutter module

设计：

- 使用 unrolled low-rank decomposition
- 每一层模拟一次 proximal update
- 对 clutter 子空间施加核范数或可学习 spectral shrinkage

形式：

\[
C^{k+1} = \mathcal{D}_{\theta_k}(X - T^k)
\]

其中 \(\mathcal{D}_{\theta_k}\) 是可学习低秩去噪算子。

作用：

- 抑制地杂波/海杂波主子空间
- 保持方法可解释性
- 与 STAP 审稿人的物理直觉一致

### 7.4 Sparse target-preserving module

设计：

- 目标被建模为 sparse and structured residual
- 使用稀疏门控，而不是把所有残差都当噪声去掉
- 使用 target preservation loss 保持注入目标或 target-only 标签

形式：

\[
T^{k+1} = \mathcal{S}_{\lambda_k}(X - C^{k+1})
\]

其中 \(\mathcal{S}_{\lambda_k}\) 是结构化 sparse operator。

### 7.5 Heavy-tailed clutter likelihood

海杂波和复杂地杂波常呈现非高斯、重尾、spiky 特性。普通 MSE 容易把 spike 当异常点处理，导致 false alarm 或过平滑。

推荐 loss：

\[
\mathcal{L}_{tail} = \sum_i \log(1 + \frac{|r_i|^2}{\nu\sigma_i^2})
\]

等价于 Student-t inspired likelihood。

可选增强：

- K-distribution inspired penalty
- adaptive scale map
- quantile loss for tail calibration

### 7.6 CFAR-calibrated detection head

模型输出不只是一张 clean RD map，而是：

- clutter estimate \(C\)
- target estimate \(T\)
- residual map \(R\)
- detection statistic \(S\)
- local threshold \(\eta\)

约束：

\[
P(S > \eta | H_0) \approx P_{fa}
\]

训练时用 clutter-only 或 target-free regions 估计 empirical false alarm。

### 7.7 总损失函数

\[
\mathcal{L} =
\lambda_1 \mathcal{L}_{rec}
+ \lambda_2 \mathcal{L}_{lr}
+ \lambda_3 \mathcal{L}_{sp}
+ \lambda_4 \mathcal{L}_{tail}
+ \lambda_5 \mathcal{L}_{tp}
+ \lambda_6 \mathcal{L}_{cfar}
+ \lambda_7 \mathcal{L}_{cons}
\]

各项含义：

- \(\mathcal{L}_{rec}\)：复值重构损失
- \(\mathcal{L}_{lr}\)：低秩或 spectral shrinkage 正则
- \(\mathcal{L}_{sp}\)：目标稀疏性正则
- \(\mathcal{L}_{tail}\)：重尾杂波似然
- \(\mathcal{L}_{tp}\)：目标保持损失
- \(\mathcal{L}_{cfar}\)：虚警校准损失
- \(\mathcal{L}_{cons}\)：增强一致性损失

### 7.8 可写入论文的理论分析

建议至少提供 2 个命题：

命题 1：目标保持边界。

在 sparse target 与 clutter subspace 互相干系数 \(\mu\) 受限时，TP-SSCS 的 shrinkage operator 对目标能量的衰减有上界。

命题 2：CFAR 校准一致性。

在 \(H_0\) clutter-only calibration set 与测试 clutter distribution 偏移有限的条件下，empirical \(P_{fa}\) 与 nominal \(P_{fa}\) 的差异可由 calibration error 和 distribution shift 上界控制。

命题 3：低秩子空间稳定性。

在扰动噪声有界时，估计 clutter subspace 与真实 clutter subspace 的夹角由 spectral gap 控制。

不需要过度数学化，但要让审稿人看到方法不是黑盒网络。

## 8. 实验矩阵

### E0：数据可用性和统计审计

目的：

- 证明所有数据确实可下载、可复现
- 给出数据规模、场景、雷达体制、标签情况
- 给出 clutter 统计分布和重尾特性

输出：

- Dataset table
- clutter histogram
- tail index / kurtosis
- range-Doppler example
- train/test split protocol

### E1：AISTAP-SIM 主性能实验

目的：

- 在有 target-only 标签的公开数据上做最强定量验证

对比：

- SMI-STAP
- diagonal loading STAP
- reduced-rank STAP
- AMF
- NAMF
- ACE
- SVD clutter cancellation
- RPCA
- GoDec
- real-valued U-Net
- complex U-Net
- complex VAE
- TP-SSCS

指标：

- \(P_d@P_{fa}=10^{-2}\)
- \(P_d@P_{fa}=10^{-3}\)
- \(P_d@P_{fa}=10^{-4}\)
- SINR gain
- SCNR gain
- target amplitude loss
- target localization error
- empirical Pfa error

顶刊过线标准：

- 在 \(P_{fa}=10^{-3}\) 下，相比最强 baseline 的 \(P_d\) 提升至少 5 个百分点
- target amplitude loss 小于 1 dB
- empirical Pfa 与 nominal Pfa 偏差小于 20 percent

### E2：RASPNet public subset 外部泛化

目的：

- 证明模型不是只适配 AISTAP-SIM
- 验证空载自适应雷达场景泛化

设置：

- 不在 RASPNet 上训练，只测试
- 或只用少量无标签 RASPNet 做 self-supervised adaptation

实验：

- zero-shot transfer
- few-shot unsupervised adaptation
- leave-domain-out test

指标：

- detection performance
- residual clutter power
- empirical Pfa
- runtime

顶刊过线标准：

- zero-shot 不明显崩溃
- self-supervised adaptation 后优于传统 baseline
- 报告失败场景，而不是只展示成功样例

### E3：IPIX 真实海杂波弱目标检测

目的：

- 证明真实海杂波下方法有效
- 解决“合成数据不可信”的审稿风险

实验：

- real target sequence detection
- clutter-only false alarm estimation
- SCR 分组
- polarization 分组
- sea state 分组

对比：

- CA-CFAR
- OS-CFAR
- SVD
- EMD
- wavelet
- RPCA
- GoDec
- complex U-Net
- TP-SSCS

指标：

- ROC
- partial AUC at low Pfa
- \(P_d@P_{fa}=10^{-3}\)
- target peak preservation
- false alarm maps

顶刊过线标准：

- 不能只有可视化
- 必须有固定 Pfa 下的 \(P_d\)
- 至少给出一个低 SCR 区间的显著优势

### E4：NetRAD 真实单/双基地海杂波压力测试

目的：

- 使用新近公开真实海杂波加强外部验证
- 验证双基地角、真实海杂波尾部和虚警校准

实验：

- clutter-only false alarm calibration
- target injection under real clutter
- leave-one-bistatic-angle-out
- tail distribution fitting before/after suppression

指标：

- empirical Pfa
- \(P_d@P_{fa}=10^{-3}\) under injected targets
- clutter tail index reduction
- residual clutter distribution
- target amplitude loss

注意：

- 如果 NetRAD 没有真实目标标签，不要声称真实目标检测
- 只声称真实 clutter 下的 controlled target injection 和 false alarm validation

### E5：跨数据集训练/测试

目的：

- 顶刊核心证据：泛化

矩阵：

| Train | Test |
|---|---|
| AISTAP-SIM | AISTAP-SIM held-out |
| AISTAP-SIM | RASPNet public |
| AISTAP-SIM | IPIX |
| AISTAP-SIM | NetRAD injection |
| IPIX clutter-only adaptation | IPIX target |
| NetRAD clutter-only adaptation | NetRAD injection |
| AISTAP-SIM + IPIX | RASPNet public |

输出：

- Cross-dataset table
- Domain shift analysis
- Failure cases

### E6：低虚警极限测试

目的：

- 直接面向雷达审稿人的核心关切

设置：

- \(P_{fa}=10^{-2}\)
- \(P_{fa}=10^{-3}\)
- \(P_{fa}=10^{-4}\)
- 如果样本数足够，尝试 \(P_{fa}=10^{-5}\)

要求：

- 每个 Pfa 至少有足够 clutter-only cells 支持估计
- 不足时明确置信区间，不虚构稳定结果

输出：

- Pd vs Pfa table
- ROC with low-Pfa zoom
- empirical vs nominal Pfa curve
- confidence interval

### E7：消融实验

必须做：

| Ablation | 目的 |
|---|---|
| 去掉复值模块，改用幅度图 | 验证复值建模价值 |
| 去掉低秩 unrolling | 验证物理结构价值 |
| 去掉 sparse target gate | 验证目标保持机制 |
| 去掉重尾 likelihood | 验证海杂波非高斯处理价值 |
| 去掉 CFAR calibration | 验证低虚警稳定性 |
| 去掉 self-supervised consistency | 验证无标签适应价值 |
| 普通 U-Net 替代 TP-SSCS | 验证不是网络容量带来的提升 |
| 不做 target injection training | 验证目标保持训练价值 |

输出：

- 主消融表
- 每个模块的 delta
- target loss vs clutter suppression trade-off

### E8：效率和部署性

目的：

- 防止审稿人质疑方法不可用

指标：

- runtime per CPI
- GPU memory
- FLOPs
- parameter count
- latency
- batch throughput

对比：

- STAP
- RPCA
- U-Net
- TP-SSCS

输出：

- Efficiency table
- Accuracy-efficiency plot

### E9：失败案例分析

必须主动报告：

- 目标完全落在 clutter ridge 上
- 极低 SCR，例如 -25 dB
- target Doppler 接近零
- 双基地角变化导致 clutter statistics 严重偏移
- 训练集中未出现的海况
- heavy-tailed spike 与目标混淆

顶刊审稿人通常更信任主动报告边界的论文。

## 9. Baseline 清单

### 9.1 传统雷达处理

- MTI
- DPCA
- CA-CFAR
- OS-CFAR
- GO-CFAR
- SMI-STAP
- diagonal loading STAP
- reduced-rank STAP
- AMF
- NAMF
- ACE

### 9.2 低秩/稀疏方法

- SVD clutter cancellation
- PCA clutter suppression
- RPCA
- GoDec
- low-rank plus sparse decomposition
- SSA

### 9.3 时频/经验分解

- wavelet denoising
- EMD
- VMD
- notch filtering

### 9.4 深度学习基线

- real-valued U-Net
- complex U-Net
- DnCNN-style denoiser
- VAE
- complex VAE
- self-supervised Noise2Void / blind-spot variant
- RASPNet-related adaptive radar transformer baseline, if code can be reproduced

### 9.5 公平性原则

所有 baseline 必须：

- 使用相同 train/test split
- 使用相同 target injection protocol
- 使用相同 Pfa calibration set
- 调参只在 validation set 上完成
- 不允许用 test set 调阈值

## 10. 评价指标

### 10.1 检测指标

- \(P_d@P_{fa}=10^{-2}\)
- \(P_d@P_{fa}=10^{-3}\)
- \(P_d@P_{fa}=10^{-4}\)
- ROC-AUC
- partial AUC at low Pfa
- F1
- precision / recall

### 10.2 杂波抑制指标

- clutter attenuation in dB
- residual clutter power
- output SCNR
- output SINR
- SINR improvement factor
- clutter ridge suppression ratio

### 10.3 目标保持指标

- target amplitude loss in dB
- target peak shift in range bin
- target peak shift in Doppler bin
- target energy preservation ratio
- target phase distortion

### 10.4 CFAR 可信度指标

- empirical Pfa
- nominal Pfa
- Pfa calibration error
- threshold stability
- false alarm spatial clustering

### 10.5 统计稳健性

- bootstrap 95 percent confidence interval
- paired test across scenarios
- leave-one-scenario-out mean and variance
- seed sensitivity

## 11. 图表设计

### Figure 1：问题定义和公开基准

内容：

- 多源公开数据
- 统一复值预处理
- target injection
- TP-SSCS
- CFAR detection
- low-Pfa evaluation

目的：

- 让审稿人一眼看到论文不是单数据集刷指标

### Figure 2：TP-SSCS 方法结构

内容：

- complex front-end
- low-rank clutter branch
- sparse target branch
- heavy-tail likelihood
- CFAR calibration head

### Figure 3：主结果可视化

内容：

- 原始 RD map
- baseline suppression
- TP-SSCS suppression
- target map
- false alarm map

### Figure 4：低虚警 ROC 和 partial AUC

内容：

- \(P_{fa}=10^{-2}\) 到 \(10^{-4}\) 区间放大
- 不同 SCR 曲线

### Figure 5：跨数据集泛化

内容：

- train/test matrix heatmap
- zero-shot vs self-supervised adaptation

### Figure 6：消融和 trade-off

内容：

- target preservation vs clutter suppression
- 去掉模块后的性能下降

### Table 1：公开数据集清单

列：

- Dataset
- Radar type
- Frequency band
- Scenario
- Label type
- Size
- Download route
- Role in this paper

### Table 2：主性能表

列：

- Method
- \(P_d@10^{-2}\)
- \(P_d@10^{-3}\)
- \(P_d@10^{-4}\)
- SINR gain
- target loss
- empirical Pfa error

### Table 3：跨数据集泛化表

列：

- Train
- Test
- Method
- Pd
- Pfa error
- target loss

### Table 4：消融表

列：

- Variant
- Removed component
- Pd drop
- SINR drop
- target loss increase
- Pfa calibration degradation

### Table 5：效率表

列：

- Method
- parameters
- FLOPs
- runtime per CPI
- GPU memory
- relative speed

## 12. 投稿过线标准

### 12.1 必须达到

- 至少 3 个公开数据源完成主实验
- 至少 10 个强 baseline
- \(P_{fa}=10^{-3}\) 下有明确优势
- 目标保持损失可量化且优于普通深度模型
- 有跨数据集泛化
- 有完整消融
- 有失败案例
- 代码和协议可开源

### 12.2 理想达到

- \(P_{fa}=10^{-4}\) 下仍有优势
- AISTAP-SIM 到 RASPNet zero-shot 不崩溃
- NetRAD target injection 表明真实 clutter 下 false alarm 稳定
- IPIX 真实目标验证优于传统海杂波检测方法
- 方法复杂度可接受

### 12.3 如果达不到则调整

如果跨数据集泛化弱：

- 把主张改成 self-supervised domain adaptation
- 不声称 universal generalization

如果低虚警不稳定：

- 强化 CFAR calibration head
- 论文主张从 detection performance 改成 clutter suppression with calibrated false alarm analysis

如果 IPIX 下载失败：

- 用 NetRAD + AISTAP-SIM + RASPNet public 做主线
- IPIX 移到 unavailable data note，不作为承诺

## 13. 代码和工程计划

### 13.1 仓库结构

推荐结构：

```text
openclutter-bench/
  configs/
  data/
    manifests/
    splits/
  docs/
  scripts/
    download/
    preprocess/
    inject_targets/
    evaluate/
    plot/
  src/
    datasets/
    baselines/
    models/
    losses/
    metrics/
  experiments/
  results/
  paper/
```

### 13.2 环境

推荐：

- Python 3.11
- PyTorch
- NumPy
- SciPy
- h5py
- scikit-learn
- matplotlib
- pandas
- hydra or yaml configs
- CUDA GPU

### 13.3 复现要求

每个实验必须有：

- config 文件
- seed
- 数据 manifest
- baseline 参数
- 输出日志
- 结果 CSV
- 图表脚本

## 14. 16 周执行时间表

### 第 1-2 周：数据下载和审计

任务：

- 下载 AISTAP-SIM
- 下载 RASPNet public subset
- 尝试下载 IPIX
- 下载 NetRAD 或选取可处理子集
- 建立数据 manifest
- 记录 hash 和访问日期

交付：

- Dataset audit report
- Preprocessing prototype
- 3 个数据源可读取样例

### 第 3-4 周：统一预处理和目标注入

任务：

- 写统一 I/Q 读取器
- 写 RD transform
- 写 target injection
- 写 split generator
- 写 CFAR evaluation skeleton

交付：

- OpenClutter-Bench v0.1
- target injection 可视化
- baseline 输入格式统一

### 第 5-6 周：传统 baseline 复现

任务：

- SVD
- RPCA
- GoDec
- STAP variants
- CFAR variants
- AMF/NAMF/ACE

交付：

- Baseline table v1
- 低虚警 evaluation 可运行

### 第 7-9 周：TP-SSCS 主模型

任务：

- complex front-end
- low-rank unrolling
- sparse target gate
- heavy-tail loss
- target preservation loss
- CFAR calibration head

交付：

- AISTAP-SIM 主结果
- 初步消融
- 训练稳定性报告

### 第 10-11 周：跨数据集泛化

任务：

- RASPNet public test
- IPIX test
- NetRAD injection test
- zero-shot / adaptation 对比

交付：

- Cross-dataset matrix
- failure cases

### 第 12 周：消融和低虚警极限

任务：

- 完整 ablation
- \(P_{fa}=10^{-4}\) 测试
- bootstrap CI
- seed sensitivity

交付：

- Ablation table
- Low-Pfa plot
- Statistical report

### 第 13 周：效率和工程性

任务：

- runtime
- FLOPs
- memory
- latency

交付：

- Efficiency table
- Accuracy-efficiency plot

### 第 14-16 周：论文写作和开源整理

任务：

- 写 TGRS 论文初稿
- 整理 figures
- 清理代码
- 写 README
- 写 data availability
- 内部审稿

交付：

- Manuscript v1
- Supplement
- Open-source package

## 15. 论文结构

### Abstract

结构：

1. 雷达杂波抑制在弱小目标和低虚警检测中的挑战
2. 现有公开数据和标签稀缺导致方法不可复现、泛化不足
3. 提出 TP-SSCS 和 OpenClutter-Bench
4. 在 AISTAP-SIM、RASPNet public、IPIX、NetRAD 上验证
5. 给出低虚警和目标保持结果

### Introduction

段落任务：

1. 雷达杂波抑制与低虚警弱目标检测的重要性
2. 现有 STAP / 统计 / 深度方法的不足
3. 公开数据和标签稀缺导致不可复现问题
4. 本文核心思路：物理引导复值自监督
5. 本文贡献

### Related Work

主题：

- STAP and adaptive radar processing
- Sea clutter statistical modeling
- Low-rank and sparse decomposition
- Deep learning for radar clutter suppression
- Self-supervised complex-valued learning
- CFAR and low-false-alarm detection

### Method

结构：

1. Problem formulation
2. TP-SSCS overview
3. Complex-valued representation
4. Low-rank clutter module
5. Sparse target preservation
6. Heavy-tailed clutter likelihood
7. CFAR calibration
8. Training objective
9. Theoretical analysis

### Experiments

结构：

1. OpenClutter-Bench setup
2. Baselines
3. Metrics
4. Main results on AISTAP-SIM
5. Cross-dataset generalization
6. Real sea clutter validation
7. Low-false-alarm analysis
8. Ablation studies
9. Efficiency and failure modes

### Discussion

必须包括：

- 为什么方法有效
- 什么时候会失败
- synthetic target injection 的边界
- 公开数据限制
- 未来是否需要真实采集数据进一步验证

## 16. Data Availability 草案

可直接改写为英文：

The experiments use publicly available radar datasets. AISTAP-SIM is available from the MIT Lincoln Laboratory GitHub repository at https://github.com/mit-ll/AISTAP-SIM. The RASPNet public CVNN/EXAMPLES subset is available from the AFRL Sensor Data Management System at https://www.sdms.afrl.af.mil/index.php?collection=raspnet. The NetRAD monostatic/bistatic sea clutter dataset is available from UCL Research Data Repository at https://rdr.ucl.ac.uk/articles/dataset/NetRAD_-_Monostatic_Bistatic_Sea_Clutter_Dataset/32676582. The IPIX sea clutter dataset is referenced through the McMaster IPIX data page at http://soma.ece.mcmaster.ca/ipix/dartmouth/datasets.html. All derived split files, target-injection metadata, preprocessing scripts, evaluation scripts and model configurations will be released with the paper.

注意：

- 不要承诺释放原始第三方数据
- 只释放下载脚本、split、metadata、预处理代码和模型代码
- 对 NetRAD/IPIX 必须遵守原数据许可和引用要求

## 17. 最大拒稿风险和应对

### 风险 1：创新被认为只是网络堆叠

应对：

- 强调 low-rank / sparse / heavy-tail / CFAR calibration 的物理约束
- 给出理论命题
- 强消融证明每个模块必要

### 风险 2：公开数据不够真实或标签不足

应对：

- AISTAP-SIM 提供 target-only 标签
- IPIX 提供真实海杂波目标验证
- NetRAD 提供真实海杂波统计和 target injection
- RASPNet public 提供外部空载场景

### 风险 3：合成目标注入被质疑

应对：

- 明确注入实验只用于 controlled stress test
- 不用注入结果替代真实目标结论
- 使用 IPIX 和 AISTAP-SIM 补真实/带标签证据

### 风险 4：跨数据集性能下降

应对：

- 把 zero-shot 和 self-supervised adaptation 分开报告
- 如果 zero-shot 不强，主张改为 unlabeled adaptation
- 报告 domain shift 和 failure cases

### 风险 5：低虚警统计不稳定

应对：

- 增加 clutter-only cells
- 报告 confidence interval
- 不报告样本量不足的 \(10^{-5}\) Pfa

## 18. 最终验收清单

投稿前必须满足：

- AISTAP-SIM 可完整复现
- RASPNet public subset 可复现
- IPIX 或 NetRAD 至少一个真实海杂波数据源完成
- 10 个以上 baseline 完成
- 低虚警 \(P_d@10^{-3}\) 有优势
- 消融表完整
- 跨数据集泛化表完整
- 失败案例完整
- 代码和配置整理完成
- 数据来源、许可和引用写清楚
- 所有图表可由脚本一键生成

## 19. 如果只能优先做一条最强路线

优先顺序：

1. AISTAP-SIM 主实验
2. TP-SSCS 主模型
3. 传统和深度 baseline
4. 低虚警检测
5. RASPNet public 外部验证
6. IPIX 真实海杂波验证
7. NetRAD 双基地海杂波 target injection
8. 开源 benchmark 包装

最低可投稿版本：

- AISTAP-SIM + IPIX + RASPNet public
- 8 个以上 baseline
- 完整消融
- 低虚警 \(P_d@10^{-3}\)
- 公开代码

顶配版本：

- AISTAP-SIM + RASPNet public + IPIX + NetRAD
- 15 个 baseline
- 完整低虚警到 \(10^{-4}\)
- 跨数据集 leave-one-domain-out
- 理论分析
- benchmark 开源

## 20. 最关键的一句话

这篇论文的成败不取决于模型名字，而取决于是否能够证明：在完全公开、可复现、低标签条件下，TP-SSCS 在低虚警弱目标保护上稳定超过强传统雷达方法和强深度学习基线，并且这种优势能够跨空载和海杂波公开数据保持。
